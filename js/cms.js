;
(function ($) {
    // TODO this may need to hook in via the tabstrip javascript library instead
    // select the 'All products' tab (first tab) as the active tab on page load.
    $('.ui-tabs-nav .tab-Product a').click();
})(jQuery);